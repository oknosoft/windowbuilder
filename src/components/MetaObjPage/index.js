import React, {Component} from 'react';
import PropTypes from 'prop-types';

class MetaObjPage extends Component {

  render() {
    return (

      <div>
        MetaObjPage
      </div>

    );
  }
}

MetaObjPage.propTypes = {
  _mngr: PropTypes.object.isRequired,
};

export default MetaObjPage;
